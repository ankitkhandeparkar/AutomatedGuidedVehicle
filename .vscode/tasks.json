{
// See https://go.microsoft.com/fwlink/?LinkId=733558
// for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and flash", 
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "west build -p auto; west flash", 
            "group": "build"
        }
    ]
}